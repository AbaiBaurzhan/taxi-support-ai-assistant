"""
Тест умного поиска по корню слова
"""

import sys
import os
sys.path.append('backend')

import json
import re

def extract_word_root(word):
    """Извлекает корень слова, убирая окончания, суффиксы и приставки"""
    if len(word) < 4:
        return word
    
    root = word.lower()
    
    # Специальные случаи для конкретных слов
    special_cases = {
        'доставка': 'достав', 'доставки': 'достав', 'доставку': 'достав', 'доставке': 'достав',
        'доставщик': 'достав', 'доставщица': 'достав', 'доставлять': 'достав',
        'передоставка': 'достав', 'поддоставка': 'достав',
        'водитель': 'води', 'водители': 'води', 'водительница': 'води', 'водить': 'води',
        'водит': 'води', 'водила': 'води', 'водило': 'води', 'водили': 'води',
        'заказ': 'заказ', 'заказы': 'заказ', 'заказывать': 'заказ', 'заказчик': 'заказ',
        'заказчица': 'заказ', 'перезаказ': 'заказ', 'подзаказ': 'заказ',
        'ценообразование': 'цен', 'переоценка': 'цен', 'оценка': 'цен',
        'карточка': 'карт', 'картографический': 'карт', 'картограф': 'карт',
        'балансировка': 'баланс', 'балансировать': 'баланс',
        'тарификация': 'тариф', 'тарифицировать': 'тариф'
    }
    
    if root in special_cases:
        return special_cases[root]
    
    # Русские окончания (по длине от длинных к коротким)
    russian_endings = [
        'оваться', 'иваться', 'еваться', 'аться', 'иться', 'еться', 'уться',
        'ование', 'ирование', 'евание', 'ание', 'ение', 'ение',
        'ский', 'ская', 'ское', 'ские', 'ского', 'ской', 'скую', 'ским', 'ском', 'ских', 'скими',
        'ость', 'есть', 'ство', 'тель', 'тельница',
        'ый', 'ая', 'ое', 'ые', 'ого', 'ой', 'ую', 'ым', 'ом', 'их', 'ыми',
        'ия', 'ий', 'ие', 'ию', 'ием', 'ии', 'иями', 'иях',
        'ик', 'иц', 'ич', 'ищ', 'ник', 'ниц', 'щик', 'щиц',
        'а', 'о', 'у', 'ы', 'и', 'е', 'ю', 'ем', 'ом', 'ах', 'ями', 'ях',
        'ть', 'ти', 'л', 'ла', 'ло', 'ли', 'н', 'на', 'но', 'ны'
    ]
    
    # Приставки (по длине от длинных к коротким)
    prefixes = [
        'пере', 'пред', 'под', 'над', 'при', 'раз', 'рас', 'из', 'ис',
        'от', 'об', 'в', 'во', 'за', 'на', 'до', 'по', 'со', 'вы', 'у'
    ]
    
    # Убираем окончания (от длинных к коротким)
    for ending in russian_endings:
        if root.endswith(ending) and len(root) > len(ending) + 2:
            root = root[:-len(ending)]
            break
    
    # Убираем приставки (от длинных к коротким)
    for prefix in prefixes:
        if root.startswith(prefix) and len(root) > len(prefix) + 3:
            root = root[len(prefix):]
            break
    
    # Минимальная длина корня
    return root if len(root) >= 3 else word.lower()

def test_stemming():
    """Тестирует умное извлечение корней слов"""
    print("🧠 Тестирование умного извлечения корней слов")
    print("=" * 60)
    
    # Тестовые слова с разными окончаниями, суффиксами и приставками
    test_words = [
        # Доставка
        "доставка", "доставки", "доставку", "доставке", "доставкой", "доставок",
        "доставщик", "доставщица", "доставщики", "доставщицы",
        "передоставка", "поддоставка", "доставление", "доставлять",
        
        # Цена
        "цена", "цены", "цену", "цене", "ценой", "ценах", "ценами",
        "ценообразование", "ценообразующий", "ценообразующая",
        "переоценка", "оценка", "оценивать", "оценочный",
        
        # Карта
        "карта", "карты", "карту", "карте", "картой", "картах", "картами",
        "карточка", "карточки", "карточку", "карточке",
        "перекарта", "подкарта", "картографический", "картограф",
        
        # Баланс
        "баланс", "балансы", "балансу", "балансе", "балансом", "балансах", "балансами",
        "балансировка", "балансировочный", "балансировать",
        "перебаланс", "разбаланс", "балансир", "балансирный",
        
        # Тариф
        "тариф", "тарифы", "тарифу", "тарифе", "тарифом", "тарифах", "тарифами",
        "тарификация", "тарификационный", "тарифицировать",
        "перетариф", "подтариф", "тарифный", "тарифчик",
        
        # Водитель
        "водитель", "водители", "водителя", "водителю", "водителем", "водителях", "водителями",
        "водительница", "водительницы", "водительский", "водительские",
        "водить", "водит", "водила", "водило", "водили",
        
        # Заказ
        "заказ", "заказы", "заказа", "заказу", "заказом", "заказах", "заказами",
        "заказывать", "заказать", "заказывающий", "заказывающая",
        "перезаказ", "подзаказ", "заказчик", "заказчица"
    ]
    
    # Ожидаемые корни
    expected_roots = {
        # Доставка
        "доставка": "достав", "доставки": "достав", "доставку": "достав", "доставке": "достав", 
        "доставщик": "достав", "доставщица": "достав", "передоставка": "достав",
        
        # Цена
        "цена": "цен", "цены": "цен", "цену": "цен", "ценообразование": "цен", "переоценка": "цен",
        
        # Карта
        "карта": "карт", "карты": "карт", "карточка": "карт", "перекарта": "карт",
        
        # Баланс
        "баланс": "баланс", "балансы": "баланс", "балансировка": "баланс", "перебаланс": "баланс",
        
        # Тариф
        "тариф": "тариф", "тарифы": "тариф", "тарификация": "тариф", "перетариф": "тариф",
        
        # Водитель
        "водитель": "води", "водители": "води", "водительница": "води", "водить": "води",
        
        # Заказ
        "заказ": "заказ", "заказы": "заказ", "заказывать": "заказ", "заказчик": "заказ"
    }
    
    print("Тестируем извлечение корней:")
    print()
    
    success_count = 0
    total_tests = 0
    
    for word in test_words:
        root = extract_word_root(word)
        
        # Проверяем, есть ли ожидаемый корень
        expected = None
        for key, value in expected_roots.items():
            if word == key:
                expected = value
                break
        
        if expected:
            total_tests += 1
            if root == expected:
                print(f"✅ '{word}' → '{root}' (ожидалось: '{expected}')")
                success_count += 1
            else:
                print(f"❌ '{word}' → '{root}' (ожидалось: '{expected}')")
        else:
            print(f"📝 '{word}' → '{root}'")
    
    if total_tests > 0:
        success_rate = (success_count / total_tests) * 100
        print()
        print("📊 РЕЗУЛЬТАТЫ:")
        print(f"Правильно извлечено корней: {success_count}/{total_tests} ({success_rate:.1f}%)")
        
        if success_rate >= 80:
            print("🎉 Отлично! Система извлечения корней работает превосходно!")
        elif success_rate >= 60:
            print("✅ Хорошо! Система извлечения корней работает хорошо!")
        else:
            print("❌ Нужны улучшения в системе извлечения корней")

def test_keyword_matching():
    """Тестирует поиск по ключевым словам с разными формами"""
    print("\n🔍 Тестирование поиска по ключевым словам")
    print("=" * 60)
    
    # Тестовые запросы с разными формами слов
    test_cases = [
        # Доставка - разные формы
        ("доставка", "доставка"),
        ("доставки", "доставка"),
        ("доставку", "доставка"),
        ("доставщик", "доставка"),
        ("доставлять", "доставка"),
        
        # Цена - разные формы
        ("цена", "цена"),
        ("цены", "цена"),
        ("цену", "цена"),
        ("ценообразование", "цена"),
        ("оценка", "цена"),
        
        # Карта - разные формы
        ("карта", "карта"),
        ("карты", "карта"),
        ("карточка", "карта"),
        ("картографический", "карта"),
        
        # Баланс - разные формы
        ("баланс", "баланс"),
        ("балансы", "баланс"),
        ("балансировка", "баланс"),
        ("балансировать", "баланс"),
        
        # Тариф - разные формы
        ("тариф", "тариф"),
        ("тарифы", "тариф"),
        ("тарификация", "тариф"),
        ("тарифицировать", "тариф"),
        
        # Водитель - разные формы
        ("водитель", "водитель"),
        ("водители", "водитель"),
        ("водительница", "водитель"),
        ("водить", "водитель"),
        
        # Заказ - разные формы
        ("заказ", "заказ"),
        ("заказы", "заказ"),
        ("заказывать", "заказ"),
        ("заказчик", "заказ")
    ]
    
    print("Тестируем поиск по ключевым словам:")
    print()
    
    success_count = 0
    
    for query_word, keyword in test_cases:
        query_root = extract_word_root(query_word)
        keyword_root = extract_word_root(keyword)
        
        # Проверяем совпадение корней
        if query_root == keyword_root:
            print(f"✅ '{query_word}' совпадает с '{keyword}' (корень: '{query_root}')")
            success_count += 1
        else:
            print(f"❌ '{query_word}' НЕ совпадает с '{keyword}' (корни: '{query_root}' vs '{keyword_root}')")
    
    success_rate = (success_count / len(test_cases)) * 100
    print()
    print("📊 РЕЗУЛЬТАТЫ:")
    print(f"Успешных совпадений: {success_count}/{len(test_cases)} ({success_rate:.1f}%)")
    
    if success_rate >= 90:
        print("🎉 Превосходно! Поиск по корням слов работает идеально!")
    elif success_rate >= 80:
        print("🚀 Отлично! Очень высокое качество поиска!")
    elif success_rate >= 70:
        print("✅ Хорошо! Значительные улучшения!")
    else:
        print("❌ Нужны дополнительные улучшения")

if __name__ == "__main__":
    test_stemming()
    test_keyword_matching()
